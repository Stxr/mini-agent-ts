# Greeter Checklist

- Script defines a `greet(name)` function.
- Script can run from command line with a fallback default name.
- README includes run instructions.
- Files are UTF-8 and easy to read.
